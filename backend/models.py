from sqlalchemy import Column, Integer, String, ForeignKey, DateTime\nfrom sqlalchemy.ext.declarative import declarative_base\nfrom sqlalchemy.orm import relationship\n\nBase = declarative_base()\n\n\nclass Level(Base):\n    __tablename__ = 'levels'\n    id = Column(Integer, primary_key=True)\n    name = Column(String, nullable=False)\n    difficulty = Column(Integer, nullable=False)  # Difficulty from 1 to 100\n    points = Column(Integer, nullable=False)\n    position = Column(Integer, nullable=False)\n\n\nclass Player(Base):\n    __tablename__ = 'players'\n    id = Column(Integer, primary_key=True)\n    username = Column(String, unique=True, nullable=False)\n    total_points = Column(Integer, default=0)\n\n\nclass Submission(Base):\n    __tablename__ = 'submissions'\n    id = Column(Integer, primary_key=True)\n    player_id = Column(Integer, ForeignKey('players.id'), nullable=False)\n    level_id = Column(Integer, ForeignKey('levels.id'), nullable=False)\n    video_link = Column(String, nullable=False)\n    status = Column(String, nullable=False)  # pending/approved/rejected\n    timestamp = Column(DateTime, nullable=False)\n    \n    player = relationship('Player', back_populates='submissions')\n    level = relationship('Level', back_populates='submissions')\n
# Relationships\nLevel.submissions = relationship('Submission', order_by=Submission.id, back_populates='level')\nPlayer.submissions = relationship('Submission', order_by=Submission.id, back_populates='player')\n